# Pip configuration for internal PyPI mirror.
# Copy to pip.conf (project root) or ~/.config/pip/pip.conf (Linux/macOS)
# or %APPDATA%\pip\pip.ini (Windows).
#
# For Docker builds: pip.conf is copied from project root (context: .).
# When only this example exists, pip uses default PyPI (index-url commented out).

[global]
# Uncomment and set for internal PyPI mirror:
# index-url = https://your-internal-pypi-mirror.example.com/simple/
# trusted-host = your-internal-pypi-mirror.example.com

# If your mirror requires authentication:
# extra-index-url = https://user:token@your-internal-pypi-mirror.example.com/simple/
